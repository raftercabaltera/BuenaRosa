import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-google-password',
  imports: [RouterLink],
  templateUrl: './google-password.html',
  styleUrl: './google-password.scss',
})
export class GooglePassword {}
